<% if Swisspay.configuration.stripe? %>
  <%= render partial: 'swisspay/shop/payment_stripe', locals: { identifier: identifier, amount: amount } %>
<% end %>

<% if Swisspay.configuration.paypal? %>
  <%= render partial: 'swisspay/shop/payment_paypal', locals: { identifier: identifier, amount: amount } %>
<% end %>

<% if Swisspay.configuration.postfinance? %>
  <%= render partial: 'swisspay/shop/payment_postfinance', locals: { identifier: identifier, amount: amount } %>
<% end %>
